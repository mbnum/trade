syntax = "proto3";

option go_package = "github.com/mbnum/trade";

package trade;

enum OrderStatus {
    open = 0;
    canceled = 1;
    done = 2;
}

enum Status {
    pending = 0;
    failure = 1;
    success = 2;
}
