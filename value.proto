syntax = "proto3";

option go_package = "github.com/mbnum/trade";

package trade;

enum Status {
    active = 0;
    inactive = 1;
}

enum OrderStatus {
    open = 0;
    canceled = 1;
    done = 2;
}
